import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';
import shadcn from 'shadcn';

export default {
  plugins: [
    tailwindcss(),
    autoprefixer(),
    shadcn(),
  ],
}
